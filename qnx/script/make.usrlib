#!/bin/sh

test_exit() {
  status=$?
  if test $status -ne 0 
    then 
      exit 
  fi 
}

log="ros.usr.lib.1.05"
dtval=`date +%Y%m%d.%H%M`


$BINPATH/makeall -vb $USR_CODEBASE src.lib/radar/ops > $LOGPATH/${log}.${dtval}
test_exit
$BINPATH/makeall -vb $USR_CODEBASE src.lib/site/site.$SD_RADARCODE >> $LOGPATH/${log}.${dtval}
test_exit



